# 资金博弈

独立于趋势诊断。趋势只评方向、起点、效率；仓位强弱在这里评。

窗口可以借用趋势的 `[起点 → 今天]`（有方向时），但算法不嵌在趋势模块里。方向为 0 时空着。

现行打分仍是区间接受度版（量大推不动 ≈ 强）。流×额 / 涨跌幅÷额 的「推过且维持」设计还没落进计算。

`仓位 = min(压力放松, 油还在)`

入口：`flow_panels` / `diagnose_flow`。
