# Monorepo convenience pin list.
# Prefer: pip install -e "./qlab[dev]" && pip install -e "./jq[dev]"
#
# Kept for environments that still want a flat requirements install.

# qlab core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyarrow>=14.0
tables>=3.9
exchange-calendars>=4.5
pyyaml>=6.0

# qlab / jq dev
pytest>=7.4
pytest-cov>=4.1
ruff>=0.1
mypy>=1.7
import-linter>=2.0

# jq connector
requests>=2.28
websocket-client>=1.6
certifi>=2023.0
